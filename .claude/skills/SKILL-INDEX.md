---
title: SKILL-INDEX（技能目录·≤200tokens·启动必读）
updated: 2026-07-05
---

# Skill Index

说出触发词即自动调用（无需打 `/`）。完整同义词在各 SKILL.md 的 `触发词:` 字段。

`ingest`进料/消化 · `scan`语料巡检 · `deep-read`深度精读/续读 · `promote`蒸馏/回填lessons · `draft`起草/出稿 · `publish`发布/配图上传 · `diagnose`诊断X类/出诊断卡 · `review`周复盘/月复盘 · `compact`压缩上下文 · `handoff`交接/新会话prompt · `merge`文档合并/去重裁定 · `trio`三件套 · `abcz`ABC分层/Z类清单 · `movement`动销率 · `priceband`价格带/毛利试算 · `posclean`清洗POS · `mdcard`出图卡 · `critique`批判审议/魔鬼代言人 · `weekly_data.sh`周报采集

**根层另有**（真身在 `/Users/davidliu/KnowledgeBase/.claude/skills/`）：`report-export`报告导出/诊断卡PDF+PNG · `read-floorplan`读平面图 · `lint-kb`/`lint-privacy`/`lint-coverage` · `process-inbox` · `zero-scheduler` → 详见根层 SKILL-INDEX。

**上线铁律**（缺一=不可用）：①SKILL.md frontmatter 有 `触发词:` 字段 ②本表已加行 ③.gitignore 已白名单（需追踪时）。
